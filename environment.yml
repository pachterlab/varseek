name: varseek
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  # - kat>=2.4.2  # only python 3.7-3.9
  - curl>=7.73.0
  - fastp>=0.23.4
  - bowtie2>=2.5.4
  - seqtk>=1.4
  - pip:
      - .  # . installs varseek and dependencies from pyproject.toml  # -r requirements.txt